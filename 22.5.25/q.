function personInfo() {
  console.log(`Name: ${this.name}, Age: ${this.age}`);
}

const person = {
  name: "Alice",
  age: 25
};

// Using call() to invoke personInfo with 'person' as the this context
personInfo.call(person);
