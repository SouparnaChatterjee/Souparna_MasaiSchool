function extractAndReverse(arr) {
    let extracted = arr.slice(2, 4);
    extracted.reverse();
    return extracted;
}
let original = [15, 30, 45, 60, 75, 90];
console.log(extractAndReverse(original)); // Output: [60, 45]
console.log(original); // Should remain unchanged
