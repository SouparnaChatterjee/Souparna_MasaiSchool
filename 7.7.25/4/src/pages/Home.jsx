import React from 'react';

const Home = () => (
  <div style={{ padding: "20px" }}>
    <h1>Welcome to the User Profile App</h1>
    <p>This app uses React Context API to manage user state globally.</p>
  </div>
);

export default Home;
