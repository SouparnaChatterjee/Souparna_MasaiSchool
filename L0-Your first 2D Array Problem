function printArr(arr)
{
  let n=arr.length;
  let m=arr[0].length;
  for(let i=0;i<n;i++)
  {
    let str="";
    for(let j=0;j<m;j++)
    {str+=arr[i][j]+" ";}
    console.log(str.trim());
  }
}
const arr=[[1,2],[3,4],[5,6]];
printArr(arr); 
