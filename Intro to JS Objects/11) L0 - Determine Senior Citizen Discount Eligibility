function checkSeniorCitizenDiscount(age) {
  if (age <= 0) {
    return "Invalid Age"; 
  }
  
  return (age >= 60) ? "Eligible for Senior Discount" : "Not Eligible for Senior Discount";
}

let age = 90;
let res = checkSeniorCitizenDiscount(age);
console.log(res);
